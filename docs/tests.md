# PyTest

This project uses pytest, pytest-mock and pytest-cov for unit testing

## Commands

* Testing, `pytest -v`
* Testing Watcher, `ptw .`
* Testing interactive, 'pytest -ia'
* Coverage, `pytest -v --cov=crocotiger_sdk`